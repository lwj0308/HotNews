<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xmcu.mynews">

    <!--
     android:usesCleartextTraffic="true" 允许使用明文通信（HTTP），Android P中默认不允许明文通信
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/toutiao"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/toutiao"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.NewsDetailActivity" />
    </application>

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />
    <uses-permission-sdk-23 android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission-sdk-23 android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission-sdk-23 android:name="android.permission.CHANGE_WIFI_STATE" />
</manifest>